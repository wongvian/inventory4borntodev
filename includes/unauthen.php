<?php

//session_start();
$session_id = session_id();

// $sqlUser = "SELECT * FROM EMPLOYEE WHERE ID =  '" . $_SESSION["admin_sms"] . "'  ";

// $stUser = oci_parse($objConnectCurr, $sqlUser);
// oci_execute($stUser, OCI_DEFAULT);
// $dataUser = oci_fetch_array($stUser, OCI_ASSOC);

// if ($session_id != $_SESSION["session_id"] || empty($_SESSION["admin_sms"]) || @$dataUser['ID'] == '') {

//     header('Location: login');
//     exit;
// }
